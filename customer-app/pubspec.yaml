name: deliberry_customer_app
description: Deliberry customer application bootstrap.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  app_links: ^6.4.1
  crypto: ^3.0.6
  http: ^1.2.2
  supabase_flutter: ^2.12.0
  shared_preferences: ^2.5.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

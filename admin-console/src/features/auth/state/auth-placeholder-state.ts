export const adminAuthPlaceholderState = {
  sections: ["credential entry", "admin boundary", "platform handoff"],
  placeholderState:
    "Admin authentication remains structural only. Real provider and session logic are intentionally deferred.",
} as const;

export const merchantAuthPlaceholderState = {
  sections: ["credential entry", "merchant handoff", "access boundary"],
  placeholderState:
    "Auth remains structural only. Real providers and session orchestration are intentionally deferred.",
} as const;

export const merchantStoreSelectionPlaceholderState = {
  sections: ["store list", "store context handoff", "selected store boundary"],
  placeholderState:
    "Store selection remains structural only. Real store lookup and persistence are intentionally deferred.",
} as const;
